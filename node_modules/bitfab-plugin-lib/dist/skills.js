/**
 * Canonical list of Bitfab plugin skills. Used by the user-prompt-submit
 * hook to detect skill invocations and by analytics to type the skill name.
 *
 * Add new skill names here and they will be picked up by the slash-command
 * regex and the {@link BitfabSkill} union type. Do not duplicate this list
 * elsewhere.
 */
export const BITFAB_SKILLS = ["setup", "assistant", "update"];
